PORT=3000

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=qiudanbu
DB_PASSWORD=change_me
DB_NAME=qiudanbu
DB_CONNECTION_LIMIT=5

UPLOAD_DIR=storage/uploads
MAX_UPLOAD_MB=5

# Optional: backend-side ticket image parser.
TICKET_PARSE_API_URL=
TICKET_PARSE_API_KEY=
TICKET_PARSE_API_MODE=openai
TICKET_PARSE_MODEL=mimov2.5pro

# Keep false on public deployments unless you trust all users who can access the page.
# Set true only if you want the browser form's "AI API URL" field to be used by the backend.
ALLOW_CLIENT_AI_ENDPOINT=false
